# 暂不展开的问题

这里保存有价值、但不属于当前里程碑的问题。记录问题和触发它的现象即可，不在这里提前写答案。

| 问题 | 为什么现在先不展开 | 计划阶段 |
| --- | --- | --- |
| 不调用 `commit()` 会发生什么？ | v0.1 只建立请求到文件的最小模型 | v0.2 / v0.3 |
| 为什么 SQLite reader 还能看到旧值？ | 需要先有事务与并发问题 | v0.4 |
| 两个人同时支付或退款会怎样？ | 当前还没有支付/退款业务和并发实验 | v0.4 |
| `BEGIN IMMEDIATE` 和普通 `BEGIN` 有什么区别？ | 属于锁与事务模式，不是当前请求链路 | v0.4 |
| 为什么要从 SQLite 换成 PostgreSQL？ | 必须先亲自遇到 SQLite 的边界 | v1.0 |
| Retry 为什么可能导致重复退款？ | 需要先出现网络失败和重试 | v1.1 / v2.1 |
| Redis、消息队列、Docker 什么时候加入？ | 必须由性能、慢任务或部署问题触发 | v1.x |
| Agent 什么时候接回来？ | 底层业务系统需要先具备真实状态和约束 | v3.0 |

## 新问题模板

```text
问题：
它由什么实验现象触发：
为什么不属于当前里程碑：
预计在哪个阶段取回：
```
