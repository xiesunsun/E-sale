# 本地实验归档

这个目录存放不再属于当前里程碑、但希望保留以便回顾的本地实验数据。

`pre-v0.1-esale.db` 是整理项目前已有的数据库，包含订单、支付以及并发相关实验痕迹。它已被 `.gitignore` 排除，不会提交到远程仓库，也不会被当前应用读取。

等课程进入事务或并发阶段时，可以把它作为历史样本查看；不要把它复制回项目根目录继续开发。当前阶段应让应用自动创建一份结构单纯的新 `esale.db`。
